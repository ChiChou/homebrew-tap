# ChiChou Homebrew Tap

## Install AirDrop CLI

```sh
brew install chichou/tap/airdrop
```

The formula builds the Objective-C implementation using the macOS Command Line Tools.
A full Xcode installation is not required.
